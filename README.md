# setup-new-machine

Configs and scripts for setting up a new machine.

## Ghostty

Config and themes live in `ghostty/`. To install on a new machine:

```sh
./scripts/setup-ghostty.sh
```

This copies the config and themes to `~/.config/ghostty/`, backing up any existing config to `config.bak` first.
